
user {
  user_id int primary auto_increment
  email string unique
}

item {
  item_id guid primary
  user_id int foreign
}

milestone {
  milestone_id int primary auto_increment
  title string
  item_id guid foreign
}

collaborator {
  collaborator_id int foreign(user_id)
  user_id int foreign(user_id)
  looped
}

item_collaborator {
  item_id guid foreign
  user_id int foreign
  joined
}

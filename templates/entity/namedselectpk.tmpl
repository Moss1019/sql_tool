@NamedStoredProcedureQuery(
	name = "select{tablenamepascal}",
	procedureName = "sp_select{tablenamepascal}",
	resultClasses = {tablenamepascal}.class,
	parameters = {
	@StoredProcedureParameter(
		mode = ParameterMode.IN,
		type = {primarykeywrapper}.class,
		name = "{primarykeyname}")
	}
),

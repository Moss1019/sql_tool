        [HttpGet("for-{primarytablelower}/{id}")]
        public ActionResult<IEnumerable<{tablepascal}View>> GetForUser([FromRoute] string id)
        {
            Guid {primarycamel};
            if(Guid.TryParse(id, out {primarycamel}))
            {
                return Ok(service.SelectForUser({primarycamel}));
            }
            return BadRequest("Malformed id");
        }
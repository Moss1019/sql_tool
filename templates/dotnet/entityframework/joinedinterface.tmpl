using {rootname}.Entities;
using System;
using System.Collections.Generic;

namespace {rootname}.Repositories
{
    public interface I{tablepascal}Repository
    { 
        {tablepascal} Insert({tablepascal} entity);

        IEnumerable<{secondarytablepascal}> Select{tablepascal}s(Guid {primarycolumncamel});

        bool Delete(Guid {primarycolumncamel}, Guid {secondarycolumncamel});
    }
}

        public IEnumerable<{tablepascal}> SelectFor{parentpascal}(Guid {primarycamel})
        {
            using var context = new GidContext(config);
            var result = context.{tablepascal}s
                .Where(x => x.{primarypascal} == {primarycamel}){childlists}
                .ToList();
            return result;
        }
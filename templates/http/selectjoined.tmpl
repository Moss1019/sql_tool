export function for{primarytablenamepascal}({primarycolumnnamecamel}: number, onSuccess: (data: any) => void, onError: (err: any) => void) {
	const url = `${SERVER_END_POINT}/api/{tablenamelower}s/for{primarytablenamepascal}/${{primarycolumnnamecamel}}`;
	return axios.get(url)
		.then(result => {
			onSuccess(result);
		})
		.catch(err => {
			onError(err);
		});
};

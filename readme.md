# SQL Tool
Many things have changed, this tool is now in the process of becoming a behemoth of power.
## updates
The table definitions are simplified, and additional options are included to give more contextual info on the tables.
Many tables can be defined in the definition file, allowing the whole DB to be generated.

Also, I realized that .md files have their own formatting and markup. Using that now to write readmes